---
title: "Programa de Prevención y Control del Dengue de Oaxaca"
output: 
  flexdashboard::flex_dashboard:
      theme: flatly
output_dir: docs
cover-image: images/cover.jpg
---


```{r setup}

# Step 1. load the librarys ####
options(shiny.maxRequestSize=30*1024^2)
library(formattable)
library(tmap)
library(rmarkdown)
library(knitr)
library(shiny)
library(flexdashboard)
library(plotly)
library(boldenr)
library(magrittr)
library(leaflet)
library(ggplot2)
library(dplyr)
library(stringr)
library(dash)
library(ECharts2Shiny)
library(sf)

# Step 2. define the path ####

if(as.vector(Sys.info()["sysname"]) == "Darwin"){
    # 2.1 dengue dataset
    path_arbo <- "/Users/fdzul/Library/CloudStorage/OneDrive-Personal/datasets/DGE/arbo/"
    
    # 2.2 vector control dataset 
    path_vect <-  "/Users/fdzul/Library/CloudStorage/OneDrive-Personal/datasets/CENAPRECE/2022/20_oaxaca"
    path_coord <- paste(path_vect, "DescargaOvitrampasMesFco.txt", sep = "/")
    
} else if(as.vector(Sys.info()["sysname"]) == "Windows"){
    # 2.1. dengue dataset
    path_arbo <- "C:/Users/HOME/OneDrive/datasets/DGE/arbo/"
    
    # 2.2 vector control dataset 
    path_vect <- "C:/Users/HOME/OneDrive/datasets/CENAPRECE/2022/20_oaxaca"
    path_coord <- paste(path_vect, "DescargaOvitrampasMesFco.txt", sep = "/")
    
} else if(as.vector(Sys.info()["sysname"]) == "Linux"){
    
}



# Step 3. load the functions ####
if(as.vector(Sys.info()["sysname"]) == "Darwin"){
    # .1 dengue dataset
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/heatmap_confirmados.R")
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/heatmap_hospitalizados.R")
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/static_bump_map.R")
    
    
    # 1.3 ovitraps dataset 
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/ovitraps_occupancy_abundancy.R")

    # 1.4 larvae control dataset
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/cl_interactive_map.R")
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/joint_cl_blocks.R")
    
    # 1.5 ulv dataset
    
    # 1.6 hotspots cases
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/risk_agebs.R")
    
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/risk_map.R")
    
    source("~/Dropbox/r_developments/r_new_functions/3.Functions/hotspots_map.R")
    
    # 1. 7 eggs hotspots ####
    
source("~/Dropbox/r_developments/r_dashboards/github_pages/test_denv_dash_oax/3.Functions/eggs_hotspots_intensity_map_year.R")

    
} else if(as.vector(Sys.info()["sysname"]) == "Windows"){
    # 1.1. dengue dataset
    
    # load the heatmap confirmados y hospitalizados function
    source('C:/Users/HOME/Dropbox/r_developments/r_dashboards/github_pages/test_dashboard/3.Functions/heatmap_confirmados.R')
    source("C:/Users/HOME/Dropbox/r_developments/r_dashboards/github_pages/test_denv_dash_yuc/functions/heatmap_hospitalizados.R")
    source("C:/Users/HOME/Dropbox/r_developments/r_new_functions/3.Functions/static_bump_map.R")
    
    # 1.3 ovitraps dataset 
    source("C:/Users/HOME/Dropbox/r_developments/r_new_functions/3.Functions/ovitraps_occupancy_abundancy.R")
    
    # 1.4 larvae control dataset
     source("C:/Users/HOME//Dropbox/r_developments/r_new_functions/3.Functions/cl_interactive_map.R")
     source("C:/Users/HOME/Dropbox/r_developments/r_new_functions/3.Functions/joint_cl_blocks.R")
    
    
    # 1.5 ulv dataset
    
    # 1.6 hotspots


    #Step  load the risk_ageb function ###
    source('C:/Users/HOME/Dropbox/r_developments/r_dashboards/github_pages/test_dashboard/3.Functions/risk_agebs.R')

    # Step  load the risk_map function ####
    source("C:/Users/HOME/OneDrive/proyects/hotspots/3.Functions/risk_map.R")

    # Step  load the hotspots map ####
    source("C:/Users/HOME/Dropbox/r_developments/r_dashboards/github_pages/test_dashboard/3.Functions/hotspots_map.R")
    
    # 1. 7 eggs hotspots ####
     source("C:/Users/HOME/Dropbox/r_developments/r_dashboards/github_pages/test_denv_dash_oax/3.Functions/eggs_hotspots_intensity_map_year.R")
    
} else if(as.vector(Sys.info()["sysname"]) == "Linux"){
    
}





# Step 4. Load the dataset ####

# 3.1 arbovirosis dataset
x <- boldenr::read_dataset_bol(path = path_arbo,
                                dataset = "sinave")
# 3.2. ovitrap dataset 

ovis <- boldenr::read_dataset_bol(path = path_vect,
                                 dataset = "vectores",
                                 inf = "Lecturas")

# 3.3  larvae control dataset
cl <- boldenr::read_dataset_bol(path = path_vect, 
                                dataset = "vectores",
                                inf = "Control") |>
    dplyr::mutate(cve_geo = paste(cve_ent, cve_mpo, 
                                  #cve_loc, # 423522
                                  sector, 
                                  Manzana, sep = ""))
cl_blocks <- joint_cl_blocks(path_vector = path_vect,
                             cve_ent = "20")

# 3.4 ulv dataset 
neb <- boldenr::read_dataset_bol(path = path_vect, 
                                 dataset = "vectores",
                                 inf = "Nebulizacion")



# Step 5. Load the RData files ####
if(as.vector(Sys.info()["sysname"]) == "Darwin"){
    # load the blocks
    load("~/OneDrive/automatic_read_ine_2010/8.RData/block_ine10_mx.RData")
    
    # load the dengue cases hotspots
    load("~/Dropbox/hotspots_2021/8.RData/cases_hotspots_agebs19.RData")
    hotspots <- cases_hotspots_agebs19 |>
    sf::st_make_valid()
    
    # data for epichannel data
    load("~/OneDrive/automatic_read_sinave/8.RData/epid_channel_data_js.RData")
    
    # dengue dataaset geocoded
    load("~/OneDrive/proyects/geocoding_mex/2022/9.RData_geocoded/den2022_positivos.RData")
    
    
    # load the vector hotspots
    load("~/OneDrive/proyects/hotspots_eggs/8.RData/20_oaxaca/betas/20_oaxaca_zinb1_betas.RData")
    
    
} else if(as.vector(Sys.info()["sysname"]) == "Windows"){
    
    # load the blocks #####
    load("C:/Users/HOME/OneDrive/automatic_read_ine_2010/8.RData/block_ine10_mx.RData") 
    
    # Step 1. load the hotspots dengue cases ####
    load("C:/Users/HOME/Dropbox/hotspots_2021/8.RData/cases_hotspots_agebs19.RData")
    
    cases_hotspots_agebs19 <- cases_hotspots_agebs19 |>
    sf::st_make_valid()
    
    # data for epichannel data
    load("C:/Users/HOME/OneDrive/automatic_read_sinave/8.RData/epid_channel_data_js.RData")
    
    # dengue dataaset geocoded
    load("C:/Users/HOME/OneDrive/proyects/geocoding_mex/2022/9.RData_geocoded/den2022_positivos.RData")
    
    ###
    load("C:/Users/HOME/OneDrive/proyects/geocoding_mex/2022/9.RData_geocoded/den_31_yucatan_2022.RData")
    
    # load the vector hotspots
    load("C:/Users/HOME/OneDrive/proyects/hotspots_eggs/8.RData/20_oaxaca/betas/20_oaxaca_zinb1_betas.RData")
    
} else if(as.vector(Sys.info()["sysname"]) == "Linux"){
    
}


```


**Vigilancia Epidemiológica**
=====================================  


**Hotspots de la Transmisión de Dengue**
===================================== 

Column {.tabset}
------------------------------------


### **<span style="color:#990000">Oaxaca</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_oaxaca}
hotspots_map(cve_ent = "20",
             locality = "Oaxaca de Juárez",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_oaxaca, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "Oaxaca de Juárez")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_oaxaca,dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_oaxaca,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_oaxaca, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_oaxaca, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>

### **<span style="color:#990000">Santiago Pinotepa Nacional</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_pinotepa}
hotspots_map(cve_ent = "20",
             locality = "Santiago Pinotepa Nacional",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_pinotepa, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "Santiago Pinotepa Nacional")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_pinotepa, dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_pinotepa,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_pinotepa, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_pinotepa, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>


### **<span style="color:#990000">Heroica Ciudad de Juchitán de Zaragoza</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_juchitan}
hotspots_map(cve_ent = "20",
             locality = "Heroica Ciudad de Juchitán de Zaragoza",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_juchitan, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "Heroica Ciudad de Juchitán de Zaragoza")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_juchitan,dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_juchitan,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_juchitan, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_juchitan, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>


### **<span style="color:#990000">Salina Cruz</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_salina_cruz}
hotspots_map(cve_ent = "20",
             locality = "Salina Cruz",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_salina_cruz, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "Salina Cruz")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_salina_cruz,dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_salina_cruz,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_salina_cruz, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_salina_cruz, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>

### **<span style="color:#990000">Santo Domingo Tehuantepec</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_santo_domingo}
hotspots_map(cve_ent = "20",
             locality = "Santo Domingo Tehuantepec",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_santo_domingo, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "Santo Domingo Tehuantepec")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_santo_domingo,dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_santo_domingo,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_santo_domingo, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_santo_domingo, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>



### **<span style="color:#990000">San Juan Bautista Tuxtepec</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_tuxtepec}
hotspots_map(cve_ent = "20",
             locality = "San Juan Bautista Tuxtepec",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_tuxtepec, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "San Juan Bautista Tuxtepec")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_tuxtepec,dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_tuxtepec,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_santo_tuxtepec, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_tuxtepec, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>

### **<span style="color:#990000">Puerto Escondido</span>**
<html>
<head>
<style>
body {
  margin: 0px;
}

.wrapper {
    display: grid;
    grid-gap: 2px;
    grid-template-columns: 280px 280px 280px 550px;
    grid-template-rows: 165px 165px 165px 165px;
    background-color: #fff;
    color: "green";
  }

  .box {
    background-color: #cfcfc4;
    color: "green";
    border-radius: 2px;
    padding: 5px;
    font-size: 100%;

  }

  .a {
    grid-column: 1 / 4;
    grid-row: 1 / 4;
  }
  .b {
    grid-column: 4 ;
    grid-row: 1 / 3;
  }
  .c {
    grid-column: 4 ;
    grid-row: 3 / 5 ;
  }
  .d {
    grid-column: 3;
    grid-row: 4;
  }
  .e {
    grid-column: 2;
    grid-row: 4;
  }
  .f {
    grid-column: 1;
    grid-row: 4;
  }
</style>
</head>
<body>
<div class="wrapper">
  <div class="box a">
```{r hotspots_dengue_puerto_escondido}
hotspots_map(cve_ent = "20",
             locality = "Puerto Escondido",
             hotspots = cases_hotspots_agebs19,
             static_map = FALSE)

```
  </div>
  <div class="box b">
```{r forest_plot_puerto_escondido, dpi = 300, fig.height= 5,fig.width = 7,fig.align = "center", out.height='100%', out.width = '100%',fig.show = "hold"}
 
# extract the locality
loc <- rgeomex::extract_locality(cve_edo = "20",
                                 locality = "Puerto Escondido")

hotspots <- cases_hotspots_agebs19[loc,]
# Logistic Regression
glm.fit <- glm(hotspots_gi ~ DENV_2008 + DENV_2009 +  DENV_2010 + DENV_2011 + 
                   DENV_2012 + DENV_2013  +  DENV_2014 + 
                   DENV_2015  +  DENV_2016  +  DENV_2019 +
                   DENV_2020 + DENV_2021,
               data = hotspots, 
               family = binomial)

result <- parameters::model_parameters(glm.fit, 
                                      exponentiate = TRUE)

plotly::ggplotly(plot(result, size_text = 1))
```
  </div>
  <div class="box c">
```{r power_law_plot_puerto_escondido,dpi = 300, warning=FALSE, fig.align = "center", out.width = '100%',out.height='100%',fig.show = "hold"}
# Step 7. make the function por calculate the cumulative ####
funcumsum <- function(x){
    x$cumulative_n <- cumsum(x$n_cases)
    x$perc_cumsum_n <- round((cumsum(x$n_cases)/sum(x$n_cases))*100, digits = 1)
    #x$cumulative_ha <- cumsum(x$ha)
    #x$perc_cumsum_ha <- round((cumsum(x$ha)/sum(x$ha))*100, digits = 1)
    x$id <- 1
    x$id_cumsum <- cumsum(x$id)
    x$id_perc_cum <- round((cumsum(x$id)/nrow(x))*100, digits = 1)
    x
}
# step 8.
data_pareto <- hotspots  |>
    sf::st_drop_geometry()  |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality")  |>
    dplyr::select(loc, n_cases, hotspots_gi)  |>
    dplyr::arrange(loc, desc(hotspots_gi), desc(n_cases))  |>
    dplyr::group_by(loc)  |>
    tidyr::nest()  |>
    dplyr::mutate(pareto_cases = purrr::map(data,
                                            funcumsum))  |>
    dplyr::select(-data)  |>
    tidyr::unnest(cols = c(pareto_cases))

# step 4. visualization of pareto rules 
plotly::ggplotly(denhotspots::power_law_plot(x = data_pareto))  |> plotly::layout(showlegend = FALSE)

```
  </div>
  <div class="box d">
```{r gauge_blocks_puerto_escondido,fig.align = "center", out.height='100%', out.width = '100%', fig.width= 1.5, fig.height=1}
y_hot <- hotspots |>dplyr::filter(hotspots_gi == 1)
centroid <- rgeomex::blocks_ine20_mx_centroid |>
  sf::st_as_sf(coords = c("x", "y"), crs = 4326)
b_loc <- centroid[hotspots, ]
b_hot <- b_loc[y_hot, ]
flexdashboard::gauge(round((nrow(b_hot)/nrow(b_loc))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Manzanas",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
  <div class="box e">
```{r gauge_AGEB_puerto_escondido, fig.align = "center", out.height='100%', out.width = '100%'}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)
  
flexdashboard::gauge(round((nrow(y_hot)/nrow(y))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "AGEBs",
      flexdashboard::gaugeSectors(success = c(0, 30), 
                                  warning = c(31, 50), 
                                  danger = c(51, 100),
                                  colors = c("success", "warning","danger")))
```
  </div>
<div class="box f">
```{r gauge_casos_puerto_escondido, out.height='90%', out.width = '90%', fig.align = "center"}

y <- hotspots  |>
    sf::st_drop_geometry() |>
    dplyr::mutate(n_cases = rowSums(dplyr::across(DENV_2008:DENV_2021)),
                  loc = "locality") |>
    dplyr::select(loc, n_cases, hotspots_gi) 
y_hot <- y |>dplyr::filter(hotspots_gi == 1)

flexdashboard::gauge(round((sum(y_hot$n_cases)/sum(y$n_cases))*100, digits = 1),
      min = 0, max = 100, 
      symbol = '%', 
      label = "Casos de Dengue",
      flexdashboard::gaugeSectors(success = c(11, 100), 
                   warning = c(6, 10), 
                   danger = c(0, 5),
                   colors = c("success", "warning","danger"))
      )
```
 </div>
</div>
</body>
</html>


**Hotspots del Vector del Dengue**
===================================== 
Column {.tabset}
------------------------------------


### **<span style="color:#990000">Oaxaca</span>**
```{r vector_hotspots_oaxaca, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20067_oaxaca_1419_zinb1,
                                 locality = "Oaxaca de Juárez",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Santa Cruz Xoxocotlán</span>**
```{r vector_hotspots_santa_cruz_xoxocotlan, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20385_santa_cruz_xoxocotlan_1419_zinb1,
                                 locality = "Santa Cruz Xoxocotlán",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Santa Lucia del Camino</span>**
```{r vector_hotspots_santa_lucia_camino, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20390_santa_lucia_camino1419_zinb1,
                                 locality = "Santa Lucia del Camino",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Matias Romero Avendaño</span>**
```{r vector_hotspots_matias_romero, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20057_matias_romero_1419_zinb1,
                                 locality = "Matias Romero Avendaño",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Loma Bonita</span>**
```{r vector_hotspots_loma_bonita, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20044_loma_bonita_1419_zinb1,
                                 locality = "Loma Bonita",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```


### **<span style="color:#990000">San Juan Bautista Tuxtepec</span>**
```{r vector_hotspots_san_juan_bautista_tuxtepec, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20184_san_juan_bautista_tuxtepec_1419_zinb1,
                                 locality = "San Juan Bautista Tuxtepec",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```


### **<span style="color:#990000">Puerto Escondido</span>**
```{r vector_hotspots_puerto_escondido, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20318_puerto_escondido_1419_zinb1,
                                 locality = "Puerto Escondido",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">San Pedro Puchutla</span>**
```{r vector_hotspots_san_pedro_pochutla, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20324_san_pedro_pochutla_1419_zinb1,
                                 locality = "San Pedro Pochutla",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Santa María Huatulco</span>**
```{r vector_hotspots_santa_maria_huatulco, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20413_santa_maria_huatulco_1419_zinb1,
                                 locality = "Santa Maria Huatulco",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Crucesita</span>**
```{r vector_hotspots_crucesita, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20413_crucesita1419_zinb1,
                                 locality = "Crucesita",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Santiago de Pinotepa Nacional</span>**
```{r vector_hotspots_santiago_pinotepa_nacional, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20482_santiago_pinotepa_nacional_1419_zinb1,
                                 locality = "Santiago de Pinotepa Nacional",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```

### **<span style="color:#990000">Putla Villa de Guerrero</span>**
```{r vector_hotspots_putla_villa, dpi = 300, warning=FALSE, message=FALSE}
eggs_hotspots_intensity_map_year(betas = betas_20073_putla_villa_1419_zinb1,
                                 locality = "Putla Villa de Guerrero",
                                 cve_ent = "20",
                                 palette = rcartocolor::carto_pal,
                                 name = "SunsetDark")
```


